# qbee 游戏自动备用限速

这个小工具会监控你配置的游戏库文件夹：

- 游戏打开时：自动打开 qbee 的“备用速度限制”
- 游戏关闭后：自动关闭 qbee 的“备用速度限制”

这里的 qbee 指 qBittorrent 或 qBittorrent Enhanced Edition，并通过 Web UI 控制。

## 第一次使用

1. 确认 qbee 已开启 Web UI。
2. 双击或运行 `qbee_game_speed_limiter.py`。
3. 第一次运行会自动生成 `qbee_game_speed_limiter.json`。
4. 打开这个 json 文件，修改这些内容：

```json
{
  "qbee_url": "http://127.0.0.1:8080",
  "username": "admin",
  "password": "adminadmin",
  "game_folders": [
    "C:\\Program Files (x86)\\Steam\\steamapps",
    "D:\\SteamLibrary\\steamapps"
  ],
  "game_processes": [],
  "exclude_processes": [
    "steam.exe",
    "epicgameslauncher.exe",
    "goggalaxy.exe",
    "wegame.exe",
    "battle.net.exe"
  ],
  "check_interval_seconds": 3,
  "restore_on_exit": true,
  "log_file": "qbee_game_speed_limiter.log"
}
```

## 怎么填两个游戏库

把 `game_folders` 改成你的两个游戏库路径，比如：

```json
"game_folders": [
  "C:\\Program Files (x86)\\Steam\\steamapps",
  "D:\\SteamLibrary\\steamapps"
]
```

程序会检查当前运行中的程序路径。只要某个运行中的 `.exe` 位于这些游戏库文件夹下面，就会打开备用速度限制。所有这些游戏关闭后，限制会关闭。

如果有个游戏不在这两个库里，也可以临时把它写到 `game_processes`：

```json
"game_processes": [
  "SomeGame.exe"
]
```

## 运行方式

在这个文件夹里运行：

```powershell
python .\qbee_game_speed_limiter.py
```

如果系统提示找不到 `python`，需要先安装 Python 3，或者让我继续帮你把它打包成可以直接双击运行的 `.exe`。

如果你想开机自动运行，可以把这个命令做成 Windows 任务计划程序，或放进启动项。

## 注意

- qbee 必须正在运行。
- qbee 的 Web UI 地址、用户名、密码必须正确。
- 如果 qbee 设置里启用了本机免登录，用户名密码通常仍然可以保留。
- 日志会写到 `qbee_game_speed_limiter.log`，方便排查。
