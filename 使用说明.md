# qbee 游戏自动备用限速

这个小工具会监控你配置的游戏库文件夹：

- 游戏打开时：自动打开 qbee 的“备用速度限制”
- 游戏关闭后：自动关闭 qbee 的“备用速度限制”

这里的 qbee 指 qBittorrent 或 qBittorrent Enhanced Edition，并通过 Web UI 控制。

## 第一次使用

1. 确认 qbee 已开启 Web UI。
2. 双击运行 `qbee_game_speed_limiter.exe`。
3. 在界面里填写 qbee Web UI 地址、用户名、密码。
4. 在“游戏库文件夹”里添加你的游戏库路径。
5. 点“保存配置”，再点“开始监控”。

配置会保存到同目录的 `qbee_game_speed_limiter.json`。你仍然可以手动修改这个 json：

```json
{
  "qbee_url": "http://127.0.0.1:8080",
  "username": "admin",
  "password": "adminadmin",
  "game_folders": [
    "C:\\Program Files (x86)\\Steam\\steamapps",
    "D:\\SteamLibrary\\steamapps"
  ],
  "game_processes": [],
  "exclude_processes": [
    "steam.exe",
    "epicgameslauncher.exe",
    "goggalaxy.exe",
    "wegame.exe",
    "battle.net.exe"
  ],
  "check_interval_seconds": 3,
  "restore_on_exit": true,
  "log_file": "qbee_game_speed_limiter.log"
}
```

## 怎么选择两个游戏库

在界面里点“添加”，选择你的两个游戏库路径，比如：

```json
"game_folders": [
  "C:\\Program Files (x86)\\Steam\\steamapps",
  "D:\\SteamLibrary\\steamapps"
]
```

程序会检查当前运行中的程序路径。只要某个运行中的 `.exe` 位于这些游戏库文件夹下面，就会打开备用速度限制。所有这些游戏关闭后，限制会关闭。

如果有个游戏不在这两个库里，也可以临时把它写到 `game_processes`：

```json
"game_processes": [
  "SomeGame.exe"
]
```

## 运行方式

直接双击 `qbee_game_speed_limiter.exe`。普通运行会打开配置界面；界面里的“开始监控”会在后台启动监控。

如果你想开机自动运行，可以把这个命令做成 Windows 任务计划程序，或放进启动项。

## 从源码重新构建

如果你修改了源码，可以在项目文件夹里运行：

```powershell
.\build.ps1
```

这会重新生成 `qbee_game_speed_limiter.exe`。

## 注意

- qbee 必须正在运行。
- qbee 的 Web UI 地址、用户名、密码必须正确。
- 如果 qbee 设置里启用了本机免登录，用户名密码通常仍然可以保留。
- 日志会写到 `qbee_game_speed_limiter.log`，方便排查。
- 密码会明文保存到本地 json 配置文件里，请不要把自己的真实配置公开上传。
